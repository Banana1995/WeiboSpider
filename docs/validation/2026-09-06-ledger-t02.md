# T01 独立复核与 T02 本机验收

日期：2026-09-06。全部为合成数据、临时 SQLite 和本机进程；未导入真实附件，未提交、推送或部署。工作开始已有大量未提交文件，均保留，未回退其他修改。

## T01 独立复核

复核 account_records.go/http、005 迁移、前端 AccountRecords、相关测试与接口文档。未发现阻断 T01 验收的实质问题：导入覆盖层保留原始行，首次更正保存原始版本及新版本，CAS/持久幂等/失败回滚与前端不确定写入恢复均有用例。未来日期作为事实允许保存是已记录边界，不是当前资产；T02 分析明确排除未来数据。同日先后此前未决定，本轮按新确认完成，不将其倒算为旧 T01 缺陷。

修改前独立运行：前端 111 项测试、build、format:check 全通过；后端 make check、make test、env -u LIQUOR_E2E_LIVE make test-e2e 全通过。原 T01 浏览器流程记录见后端设计；本轮又实际执行了账户级更正并验证原始版本保留。

## T02 交付

- 新增 006_analysis_basis.sql：不可变稳定排序映射、账户/轨道变更日志、估值采样水位关联。001–005 不改写。
- 新增 GET/HEAD /accounts/{id}/analysis-basis，强制指定 reported 或 holdings。返回精确资产/资金流、期初期末、同日选值、来源 ID/版本/日期、投影指纹和潜在失效区间。
- 账户级同行资产为资金进出之后；缺资产资金流只沿用前置明确资产原额，不加现金流、不用未来值、不写新观察。无首值不可用，零与空不同。
- 列表及有效摘要按日期和稳定序号，同日最后值胜出。导入来源行序，手工创建序，更正不改序号；旧持久回执及修订不重写。
- 持仓修改只影响相关旧/新账户及转账双方；采样前捕获水位，网络期间修改可追踪。reported 不污染 holdings；无关账户不误报估值过期。
- Vue 增加按需查看的分析依据面板、区间与独立轨道选择、来源/沿用/顺序说明及变更提示。写入和估值刷新后已打开面板更新，保留读取竞态保护。没有新增图表或收益计算。
- 需求、设计、路线图与 API 记录公网匿名共享全部读写的最新决定，不再将访客写权限列为未决；未改生产或回环保护，未增加认证。

## 自动验证

最终命令均通过：

```text
backend: make check && make test && env -u LIQUOR_E2E_LIVE make test-e2e
frontend: npm test && npm run build && npm run format:check
git diff --check
```

前端 114 项测试、11 个测试文件。新增 LedgerAnalysisBasis 的 3 项测试覆盖精确金额/缺期初/沿用提示、同范围水位刷新、轨道隔离、响应身份校验、切换账户迟到请求和读取失败清空。既有 111 项全部保持通过。

后端新增 7 项分析测试覆盖：空账户、首笔仅资金流、明确零、组合行 post-flow、日志、超安全整数与超过 int64 的合计、未来截止拒绝、补记不能向未来取值、同日稳定顺序、更正/改期/作废、导入原序和重复确认、混合轨道、潜在影响与无后续变化、失败回滚、005 升级后原始导入/回执不变、旧观察 untracked、采样中修改及晚保存、相关/无关账户、转账旧新四账户、未来业务变动不污染更早观察。修改原摘要断言以适配已确认的同日规则，迁移数量更新为 6。

新增真实进程 e2e 验证 T02 投影、稳定序号及水位跨重启一致，作废资产后沿用变 unavailable 且此前依据标为受影响。既有后端所有模块 race/shuffle 和进程级回归通过；真实上游用例明确跳过。

## 浏览器证据

独立新演示地址：`http://127.0.0.1:18572/ledger`，后端 `127.0.0.1:18571`，只绑定回环，LIQUOR_AUTO_SYNC=false，上游设为不可连接的本机地址。未停止或修改原 T01 演示 18562。

合成演示操作及结果：

1. 新空库创建 Synthetic T02，按顺序写入同日资产 100、200，次日资金流 50 但资产空，另写未来日期 999。
2. 有效全日期摘要保留未来 999，分析默认截止今天只得 200（carried），净流入 50；不成为 250，也不引用未来 999。来源为同日最后明确记录 manual-a/v1。
3. 通过网页选择 manual-a，更正资产为 250。分析自动刷新为沿用 250、来源 v2，原稳定序号不变；显示上次依据受变更影响。明细中的资金流 total_assets 仍为空，不产生新观察；旧版本在修订区保留。
4. 移动端筛选单日 2020-01-02，opening=250、closing=250、net_flow=50；含首尾资金流与前置期初符合接口。
5. 桌面 1365×900、移动 390×844 实测 documentElement.scrollWidth 分别为 1365、390，无页面横向溢出；表单和来源文本可用。分析接口 200，无 JS 异常；唯一观察到的资源 404 为未导入账户的 import-summary，页面正确显示“尚未导入”，不是分析故障。

截图与本机日志仅存于临时目录，不进入仓库：

```text
/var/folders/xk/9mnrvs4j3ksf_dcp2725mtt80000gn/T/opencode/ledger-t02.lcqphr/
  desktop.png
  mobile.png
  backend.log
  vite.log
```

## 限制与下一项

- T02 仅生成当前投影、识别潜在失效区间，不重算历史行情，不覆盖旧观察；stale 留待 T06，迁移前无水位的观察为 untracked。
- 失效范围自最早受影响日期向后开放，是潜在依赖范围，不承诺每一天金额都改变。当前没有持久化收益缓存或大规模分析分页。
- 一次分析最多读取 10,000 条截至 cutoff 的账户记录/观察和 10,000 条水位后的变更，超限明确拒绝而非截断；持仓沿用现有全账本重放机制。
- 旧 005 混合数据升级采用导入 batch/source_row 在前、手工原始 rowid 在后的确定化规则；不臆测真实日内时间。旧列表游标需从首页刷新；稳定序号允许间隔，不能当作记录数。
- 无期初仍不可做完整区间收益；Modified Dietz/XIRR/TWR 均未实现。公网配置、真实数据与发布仍受 G01–G03 门禁约束，不需要新增认证。
- T01/T02 可标记完成；下一项固定 T03，本轮未开始 T03。
